{
  "name": "vue-memory-game",
  "version": "1.0.0",
  "description": "一个用Vue编写的记忆翻牌小游戏",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build --config vite.config.single.js",
    "build:standalone": "npm run build:single && node build-inline.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}

